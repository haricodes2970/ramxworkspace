[project]
name = "ramxworkspace-api"
version = "0.1.0"
description = "Backend foundation for RamxWorkspace"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
